# 接口差距清单

> 目标：把当前后端从“主体接口已打通”推进到“前端核心页面可关闭 Mock 联调、演示数据闭环稳定”。
> 对照来源：`前端接口文档-后端版.md`、当前 `AI-web/controller`、当前 VO/Service 实现。

## 状态总览

| 模块 | 当前状态 | 主要差距 | 优先级 |
|---|---|---|---|
| 认证 | 已完成登录、登出、用户信息 | 已支持 BCrypt 兼容校验和真实角色查询 | P2 |
| 首页 Dashboard | 已完成概览接口 | 空数据演示默认值已移除 | P2 |
| 生产排产 | 主体已通，有订单、生成、详情、历史、导入、日期查询、方案对比 | 核心演示闭环已补齐 | P0 |
| 能源运行 | 已完成实时数据、生成方案、详情、历史、设备、预测、趋势、分析、碳减排接口 | 核心 Mock 接口已落地 | P0 |
| 协同优化 | 已完成生成、结果、任务、冲突、评价、对比、帕累托接口 | 核心联调接口已补齐 | P1 |
| 实时调控 | 已完成导入、最新、历史 | 导入响应已兼容 `success/controlId` | P1 |
| 数据报表 | 已完成优化效果、能源分析、趋势、碳减排和 CSV 导出 | 核心报表接口已补齐 | P1 |
| 系统管理 | 已完成用户、角色、日志、配置、数据点接口 | 后台管理基础闭环已补齐 | P2 |
| 测试与交付 | 已补最小测试、服务级测试、数据库集成测试和启动导数说明 | 可继续扩展真实 MySQL 联调测试 | P1 |

## P0：先保证核心演示闭环

### 1. 生产排产：按日期查询方案

- 状态：已完成。
- 新增接口：`GET /api/production/schedules/date/{date}`
- 查询逻辑：
  - 按 `production_schedule_plan.schedule_date = date` 找最新方案。
  - 查该方案下 `production_schedule_detail` 小时明细。
  - 返回方案摘要、24 小时任务列表、产线负荷率。
- 建议返回字段：
  - `scheduleId`
  - `scheduleName`
  - `scheduleDate`
  - `planStartTime`
  - `totalProduction`
  - `totalEnergy`
  - `avgLoadRate`
  - `details`
- 已实现：
  - `ProductionController#getScheduleByDate`
  - `ProductionScheduleService#getPlanByDate`
  - 按日期查询最新 `production_schedule_plan`
  - 复用排产详情 VO 返回小时明细和派生指标
- 验收标准：
  - 前端日计划视图关闭 Mock 后能按日期展示真实方案。
  - 无方案时返回清晰 `404`，提示先导入或生成排产方案。

### 2. 生产排产：详情字段补齐

- 状态：已完成。

- 当前接口：`GET /api/production/schedules/{scheduleId}`
- 当前已有：
  - `scheduleId`
  - `scheduleName`
  - `planStartTime`
  - `ecBaseline`
  - `ecOptimized`
  - `ecReduction`
  - `optimalTemperature`
  - `optimalSpeed`
  - `totalProduction`
  - `totalEnergy`
  - `details`
- 已补充派生字段：
  - `energySavingsRate`：可由 `ecBaseline/ecOptimized` 推导。
  - `avgLoadRate`：可由小时电耗与峰值/额定值推导。
  - `deadlineCompliance`：演示阶段可从订单完成率或固定业务规则派生。
  - `lineId`、`lineName`：如明细表暂缺真实产线，可从默认产线或生产线表补齐。
- 已完成：
  - `SchedulePlanVO` 返回 `energySavingsRate/avgLoadRate/deadlineCompliance`。
  - `ScheduleDetailVO` 返回 `lineId/lineName/equipmentLoadRate`。
  - `ProductionScheduleServiceImplTest` 覆盖详情派生字段计算和产线兜底。
- 验收标准：
  - 前端方案预览卡片不再出现核心指标为 0。

### 2.1 生产排产：方案对比

- 状态：已完成。
- 新增接口：`POST /api/production/schedules/compare`
- 请求字段：
  - `scheduleIds`：排产方案 ID 列表，首个 ID 作为基准方案。
- 已实现：
  - `ScheduleCompareDTO`
  - `ScheduleCompareVO`
  - `ScheduleCompareItemVO`
  - `ProductionController#compareSchedules`
  - `ProductionScheduleService#compare`
- 返回能力：
  - 每个方案的产量、电耗、EC、节能率、平均负荷率、交期达成率。
  - 相对基准方案的产量差、电耗差、电耗变化率。
  - 自动标出总电耗最低方案、EC 最低方案、最大节能率。
- 验收标准：
  - 前端方案对比页可传多个方案 ID 并渲染真实对比数据。

### 3. 能源方案详情展示字段

- 状态：已完成。
- 当前接口：`GET /api/energy/plan/{planDate}`
- 当前 `EnergyPlanVO` 只有成本和明细。
- 已补：
  - `loadForecast`
  - `optimizationResult`
  - `strategies`
  - `pricePolicy`
- 实现方式：
  - `loadForecast` 从 `energy_plan_detail` 24 小时数据聚合：
    - `peakHour` / `peakLoad`
    - `valleyHour` / `valleyLoad`
    - `avgLoad`
  - `optimizationResult` 从成本和负荷曲线派生：
    - `estimatedSavings`
    - `totalCost`
    - `originalCost`
    - `peakShavingRate`
  - `strategies` 根据峰谷电价和负荷峰值生成 3-5 条策略。
  - `pricePolicy` 返回演示用峰平谷电价：
    - 谷：`00:00-08:00`
    - 平：`08:00-18:00,22:00-24:00`
    - 峰：`18:00-22:00`
- 已实现：
  - `LoadForecastVO`
  - `EnergyOptimizationResultVO`
  - `EnergyStrategyVO`
  - `PricePolicyVO`
  - `PricePolicyPeriodVO`
  - `EnergyPlanServiceImpl#getPlanDetail` 聚合返回上述字段。
- 验收标准：
  - 前端能源方案页面不再用写死 Mock 展示核心卡片。

### 4. 能源运行：高频 Mock 接口落地

优先实现这些接口，全部可从现有表派生，不必等待算法组：

| 接口 | 数据来源 | 做法 |
|---|---|---|
| `GET /api/energy/plans/history` | `energy_plan` | 已完成：分页返回历史方案 |
| `GET /api/energy/device-status` | `energy_equipment` + 最近明细 | 已完成：返回设备运行状态、负荷、告警等级 |
| `GET /api/energy/load-forecast` | `energy_plan_detail` 或 `energy_realtime_data` | 已完成：返回小时负荷预测 |
| `GET /api/energy/consumption-trend` | `energy_realtime_data` | 已完成：按小时/日聚合电、蒸汽、碳排和估算成本 |
| `GET /api/energy/analysis` | `evaluation_metric` + `energy_realtime_data` | 已完成：返回成本、能耗、碳排、功率因数和最近评价指标 |
| `GET /api/energy/trend` | `report_statistic` | 已完成：返回能耗、成本、降本、碳减排、产量趋势 |
| `GET /api/energy/carbon-reduction` | `report_statistic` | 已完成：返回碳减排和累计碳减排汇总 |

已新增：

- `EnergyDeviceStatusVO`
- `EnergyLoadForecastVO`
- `EnergyLoadForecastPointVO`
- `EnergyConsumptionTrendVO`
- `EnergyConsumptionTrendPointVO`
- `EnergyAnalysisVO`
- `EnergyTrendVO`
- `EnergyTrendPointVO`
- `EnergyCarbonReductionVO`
- `EnergyCarbonReductionPointVO`
- `EnergyPlanService#listHistory`
- `EnergyPlanService#getDeviceStatus`
- `EnergyPlanService#getLoadForecast`
- `EnergyPlanService#getConsumptionTrend`
- `EnergyPlanService#getAnalysis`
- `EnergyPlanService#getTrend`
- `EnergyPlanService#getCarbonReduction`

验收标准：

- 前端能源运行主要页面关闭 Mock 后有真实接口响应。
- 每个接口空数据时返回空列表或 404，避免前端白屏。

## P1：补协同优化和报表能力

### 5. 协同优化结果结构对齐

- 状态：已完成。
- 当前接口：
  - `POST /api/optimize/joint/generate`
  - `GET /api/optimize/joint/{id}`
- 当前返回更偏后端内部结构：
  - `optimizeId`
  - `costReductionRate`
  - `energyReductionRate`
  - `executeRate`
  - `mape`
  - `ec`
  - `er`
  - `conflicts`
  - `timeSeries`
- 前端期望结构：
  - `id`
  - `name`
  - `status`
  - `objectives`
  - `constraints`
  - `optimizationResult`
  - `productionPlan`
  - `energyPlan`
- 建议做法：
  - 保留当前字段，新增前端期望字段，避免破坏已有调用。
  - `optimizationResult` 汇总当前降本率、降耗率、ER、MAPE。
  - `productionPlan` 从排产方案和明细生成摘要。
  - `energyPlan` 从能源方案和明细生成摘要。
- 已实现：
  - `JointOptimizeVO` 新增 `id/name/status/objectives/constraints/optimizationResult/productionPlan/energyPlan`。
  - `JointOptimizationResultSummaryVO`
  - `JointProductionPlanSummaryVO`
  - `JointEnergyPlanSummaryVO`
  - `JointOptimizationServiceImpl#getResult` 组装前端摘要结构，同时保留原有指标、冲突和时序字段。
- 验收标准：
  - 协同优化详情页能直接渲染真实接口返回。

### 6. 协同优化附属接口

| 接口 | 优先级 | 做法 |
|---|---|---|
| `GET /api/optimize/tasks` | P1 | 已完成：从 `algorithm_task` 查 `JOINT_OPTIMIZATION` |
| `GET /api/optimize/conflicts?taskId=xxx` | P1 | 已完成：支持按 `taskId` 或 `optimizeId` 从 `constraint_conflict` 查询 |
| `GET /api/optimize/evaluation/{id}` | P1 | 已完成：从 `joint_optimization_plan` 组装 MAPE、EC、ER、冲突数、时序点数 |
| `POST /api/optimize/compare` | P1 | 已完成：多方案降本率、降耗率、MAPE、EC、ER 指标对比 |
| `GET /api/optimize/pareto-frontier` | P2 | 已完成：从历史协同优化方案派生降本率-降耗率帕累托散点 |

已新增：

- `JointOptimizationService#listTasks`
- `JointOptimizationService#listConflicts`
- `OptimizeController#listTasks`
- `OptimizeController#listConflicts`
- `ConflictVO` 扩展 `conflictId/optimizeId/resolved`
- `JointOptimizeCompareDTO`
- `JointOptimizeEvaluationVO`
- `JointOptimizeCompareVO`
- `JointOptimizeCompareItemVO`
- `JointOptimizationService#getEvaluation`
- `JointOptimizationService#compare`
- `OptimizeController#getEvaluation`
- `OptimizeController#compare`
- `JointParetoPointVO`
- `JointParetoFrontierVO`
- `JointOptimizationService#getParetoFrontier`
- `OptimizeController#getParetoFrontier`

### 7. 报表接口

| 接口 | 数据来源 | 优先级 |
|---|---|---|
| `GET /api/reports/energy-analysis` | `report_statistic` + `evaluation_metric` | 已完成 |
| `GET /api/reports/energy-trend` | `report_statistic` | 已完成 |
| `GET /api/reports/carbon-reduction` | `report_statistic` | 已完成 |
| `GET /api/reports/export/{type}` | 查询结果导出 CSV/Excel | 已完成：CSV 导出 |

验收标准：

- 报表页面的核心图表数据来自后端，而不是前端写死。

已新增：

- `ReportEnergyAnalysisVO`
- `ReportService#getEnergyAnalysis`
- `ReportService#getEnergyTrend`
- `ReportService#getCarbonReduction`
- `ReportController#getEnergyAnalysis`
- `ReportController#getEnergyTrend`
- `ReportController#getCarbonReduction`
- `ReportService#export`
- `ReportController#export`
- 支持导出类型：`optimization-effect`、`energy-analysis`、`energy-trend`、`carbon-reduction`

### 8. 实时调控响应字段对齐

- 状态：已完成。
- 当前接口已可用：
  - `POST /api/realtime-control/import`
  - `GET /api/realtime-control/latest`
  - `GET /api/realtime-control/history`
- 已完成：
  - `RealtimeControlImportResultVO` 保留 `taskId/latestControlId/insertedCount/updatedCount/totalCount`。
  - 兼容增加 `success/controlId`。
  - `RealtimeControlServiceImpl#importRealtimeControl` 导入成功时返回 `success=true`，`controlId=latestControlId`。
- 验收标准：
  - 前端导入成功提示和后续查询都能拿到调控 ID。

## P2：交付质量和后台管理

### 8.1 首页 Dashboard 空数据语义

状态：已完成。

- 已完成：空库或缺数据时不再返回 `92.5/81.6/95.2` 等演示默认值。
- 已完成：生产进度、能源负荷率、方案执行率无真实来源时返回 `0`。
- 已完成：新增 `DashboardServiceImplTest` 固化空数据返回 0 的行为。

### 9. 系统管理接口

状态：已完成。

| 功能 | 方法 | 路径 | 做法 |
|---|---|---|---|
| 用户列表 | GET | `/api/system/users` | 已完成：查 `sys_user`，关联 `sys_user_role/sys_role`，不返回密码 |
| 创建用户 | POST | `/api/system/users` | 已完成：写 `sys_user`，支持绑定角色 |
| 更新用户 | PUT | `/api/system/users/{id}` | 已完成：更新基础信息、状态和角色 |
| 删除用户 | DELETE | `/api/system/users/{id}` | 已完成：逻辑删除用户并清理角色关系 |
| 角色列表 | GET | `/api/system/roles` | 已完成：查 `sys_role`，支持状态筛选 |
| 操作日志 | GET | `/api/system/logs` | 已完成：分页查 `sys_operation_log` |
| 系统配置 | GET/PUT | `/api/system/config` | 已完成：查改 `system_config`，只允许修改 `editable=1` 配置 |
| 数据点列表 | GET | `/api/system/data-points` | 已完成：从生产产线、生产设备、能源设备基础表派生 |

### 10. 安全与配置

- 状态：已完成。
- `application.yml` 改造：
  - 已完成：`DB_PASSWORD` 不写死，默认从环境变量读取。
  - 已完成：`JWT_SECRET` 从环境变量读取，并在 `JwtUtils` 中实际生效。
  - 已完成：`JWT_EXPIRE_SECONDS` 从环境变量读取，并在 `JwtUtils` 中实际生效。
- 编码：
  - 已完成：确认源码、SQL、Markdown 按 UTF-8 保存。
  - 已完成：新增 `.editorconfig`，父 `pom.xml` 显式设置源码/报告/测试编码为 UTF-8。
  - 已完成：`启动说明.md` 增加 PowerShell 中文乱码处理方式。
- 登录：
  - 已完成：兼容历史明文密码登录。
  - 已完成：新增/更新用户密码写入 BCrypt 哈希。
- 角色：
  - 已完成：登录和用户信息接口从 `sys_user_role/sys_role` 查询角色。
  - 已完成：`LoginVO` 保留 `role` 主角色，同时增加 `roles` 角色列表。
- 鉴权：
  - 已完成：`AuthInterceptor` 对 `/api/**` 启用 JWT 校验，登录、文档和 `OPTIONS` 预检放行。
  - 已完成：系统管理、生产排产、能源运行按角色拦截，未登录返回 `401`，无权限返回 `403`。
- 异常示例：
  - 已完成：新增 `接口异常返回示例.md`，说明统一 `code/message/data` 响应和前端处理建议。

### 11. 测试

状态：首批冒烟测试已完成。

已完成：

- `AI-web` 增加 `spring-boot-starter-test` 测试依赖。
- 父 `pom.xml` 增加 `maven-surefire-plugin 3.2.5`，确保 JUnit 5 测试执行。
- 父 `pom.xml` 增加 `maven-compiler-plugin` 的 `parameters=true`。
- 新增 `ControllerSmokeTest`，使用 `MockMvc` standalone 模式，不依赖数据库启动。
- 测试覆盖：
  - `POST /api/auth/login`
  - `GET /api/energy/load-forecast`
  - `GET /api/optimize/evaluation/{id}`
  - `GET /api/reports/energy-analysis`
  - `AuthServiceImpl` 登录成功、BCrypt 密码、密码错误、禁用账号、当前用户和角色兜底
  - `DashboardServiceImpl` 空数据语义
  - `ProductionScheduleServiceImpl` 生成方案、导入 daily_plan、排产详情派生字段
  - `EnergyPlanServiceImpl` 能源方案详情、设备状态、负荷预测、能耗趋势、分析摘要、趋势报表和碳减排累计
  - `JointOptimizationServiceImpl` 协同优化评价、方案对比和帕累托前沿
  - `JwtUtils` 动态密钥和过期时间配置
  - `RealtimeControlServiceImpl` 实时调控 JSON 导入、插入/更新计数和兼容字段
  - `ReportServiceImpl` 能源分析聚合、碳减排累计和 CSV 导出
  - `SystemServiceImpl` 用户创建加密、角色同步、删除清理和只读配置保护
  - `AuthInterceptor` Token 缺失、角色越权、角色放行和 `OPTIONS` 预检
  - `DatabaseIntegrationTest` H2 内存库验证用户角色关联、能源方案明细聚合、报表碳减排累计
- 测试过程中修复：
  - `EnergyController#getLoadForecast` 的 `@RequestParam` 显式参数名。
  - `OptimizeController#listConflicts` 的 `@RequestParam` 显式参数名。
  - `ProductionController#listScheduleHistory` 的 `@RequestParam` 显式参数名。

后续建议继续补：

| 测试 | 覆盖点 |
|---|---|
| `AuthServiceTest` | 已完成：登录成功、BCrypt 密码、密码错误、禁用账号、当前用户和角色兜底 |
| `ProductionScheduleServiceTest` | 已完成：生成方案、导入 daily_plan、查详情、导入格式校验 |
| `EnergyPlanServiceTest` | 已完成：方案详情、设备状态、负荷预测、能耗趋势、分析摘要、趋势报表、碳减排累计 |
| `JointOptimizationServiceTest` | 已完成：评价指标、方案对比、帕累托前沿 |
| `RealtimeControlServiceTest` | 已完成：导入 JSON、插入/更新计数、`success/controlId` 兼容字段 |
| `ReportServiceTest` | 已完成：报表聚合、碳减排累计、CSV 导出 |
| `SystemServiceTest` | 已完成：创建用户、角色同步、删除用户、只读配置拦截 |
| `AuthInterceptorTest` | 已完成：未登录 401、越权 403、角色放行、预检放行 |
| `DatabaseIntegrationTest` | 已完成：H2 内存库核心表结构、关联查询和聚合查询 |
| `ControllerSmokeTest` | 主要 `/api/**` 接口响应格式 |

验收命令：

```powershell
mvn test
mvn -DskipTests package
```

### 12. 启动和导数文档

状态：已完成。

新增 `启动说明.md`，包含：

1. 创建数据库。
2. 执行 `数据库建表SQL.sql`。
3. 执行 `reset_all.sql`。
4. 按顺序导入：
   - `seed_demo_data.sql`
   - `seed_steel_data.sql`
   - `seed_steel_1min.sql`
   - `seed_mpc_control.sql`
   - `seed_mpc_hourly.sql`
5. 配置环境变量：
   - `DB_URL`
   - `DB_USERNAME`
   - `DB_PASSWORD`
   - `JWT_SECRET`
6. 启动后端。
7. 访问 Knife4j / Swagger。
8. 前端关闭 Mock 后联调。

## 推荐开发顺序

1. 补 `GET /api/production/schedules/date/{date}`。
2. 补排产详情派生字段和 `POST /api/production/schedules/compare`。
3. 扩展 `EnergyPlanVO`，补能源方案详情四组展示字段。
4. 补能源模块 7 个 Mock 接口。
5. 对齐协同优化详情结构。
6. 补协同优化任务、冲突、评价、对比接口。
7. 补报表能源分析、趋势、碳减排接口。
8. 兼容实时调控导入响应字段。
9. 整理 `application.yml` 和启动/导数说明。
10. 补最小测试集。

## 完成标准

- 前端核心页面可设置 `VITE_USE_MOCK=false` 联调。
- 排产、能源、协同优化、实时调控、报表可以形成演示闭环。
- 空库能按文档建表导数并启动。
- `mvn test` 或至少 `mvn -DskipTests package` 通过。
- Git 工作区只包含本次确认过的代码、SQL、文档改动。
