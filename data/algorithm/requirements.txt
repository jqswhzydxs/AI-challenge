# Python runtime requirements

Python >= 3.10

# No third-party Python packages are required by generate_plan.py.
# The current implementation uses only the Python standard library.

# Optional future dependencies, only if the heuristic scheduler is replaced
# with a strict MILP solver:
# scipy
# numpy
# ortools
