# 今日修改审查说明

> 审查范围：本文件整理今天围绕“源码中文乱码、数据库集成测试、统一异常返回示例、登录权限拦截/角色鉴权”完成的修改。

## 1. 总览

今天的修改主要分为四类：

| 类别 | 目标 | 主要文件 |
|---|---|---|
| 编码与中文显示 | 明确项目统一使用 UTF-8，避免 PowerShell/编辑器误显示乱码 | `.editorconfig`、`pom.xml`、`启动说明.md` |
| 数据库集成测试 | 用 H2 内存库验证核心表结构、关联查询和聚合查询 | `DatabaseIntegrationTest.java`、`AI-web/pom.xml` |
| 统一异常返回示例 | 给前端和答辩说明统一错误响应结构 | `接口异常返回示例.md` |
| 登录鉴权与角色拦截 | `/api/**` 默认需要 Token，并按角色限制部分模块 | `AuthInterceptor.java`、`WebMvcConfig.java`、`AuthInterceptorTest.java` |

当前验证结果：

```powershell
mvn test
```

已通过：`35` 个测试，`0` 失败，`0` 错误。

## 2. 中文乱码与编码处理

### 修改文件

- `.editorconfig`
- `pom.xml`
- `启动说明.md`

### 审查结论

今天先确认了一个关键点：Java 源码、SQL、Markdown 文件按 UTF-8 读取时中文内容是正常的。之前终端里看到的 `鐢熶骇`、`鑳芥簮` 之类乱码，是 PowerShell 输出编码导致的显示问题，不是文件内容已经损坏。

### 做了什么

新增 `.editorconfig`：

- 全项目默认 `charset = utf-8`
- Java/XML/YAML/Markdown/SQL/JSON 明确 UTF-8
- 统一换行和尾部空白规则

更新父 `pom.xml`：

- 增加 `project.reporting.outputEncoding=UTF-8`
- Maven 编译插件显式指定 `<encoding>UTF-8</encoding>`
- Surefire 测试 JVM 增加 `-Dfile.encoding=UTF-8`

更新 `启动说明.md`：

- 增加 PowerShell 中文显示修复命令：

```powershell
chcp 65001
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
```

### 怎么审查

1. 用支持 UTF-8 的编辑器打开 `.java`、`.md`、`.sql` 文件，确认中文正常。
2. 查看 `.editorconfig` 是否符合项目风格。
3. 查看父 `pom.xml` 的编码配置是否在 `<properties>`、`maven-compiler-plugin`、`maven-surefire-plugin` 中生效。

### 注意点

这次没有批量重写所有 Java 文件，原因是源码内容没有损坏。批量重写反而容易制造大量无意义 diff。

## 3. 登录鉴权与角色拦截

### 修改文件

- `AI-web/src/main/java/com/xq/web/config/AuthInterceptor.java`
- `AI-web/src/main/java/com/xq/web/config/WebMvcConfig.java`
- `AI-web/src/test/java/com/xq/web/config/AuthInterceptorTest.java`

### 原问题

之前 `AuthInterceptor` 虽然已经注册到 `/api/**`，但逻辑只是记录日志：

- 有 Token：放行
- 无 Token：也放行

这意味着后端实际上没有登录保护，任何请求都能访问。

### 现在的行为

除以下路径/请求外，`/api/**` 默认需要携带：

```http
Authorization: Bearer <token>
```

放行项：

- `POST /api/auth/login`
- Swagger/OpenAPI 文档路径
- `OPTIONS` 预检请求

鉴权失败返回统一 JSON：

```json
{
  "code": 401,
  "message": "未登录或 Token 已失效",
  "data": null
}
```

角色无权限返回：

```json
{
  "code": 403,
  "message": "当前角色无权访问该接口",
  "data": null
}
```

### 当前角色规则

| 路径前缀 | 允许角色 |
|---|---|
| `/api/system/**` | `SYSTEM_ADMIN` |
| `/api/production/**` | `SYSTEM_ADMIN`, `PRODUCTION_DISPATCHER` |
| `/api/energy/**` | `SYSTEM_ADMIN`, `ENERGY_MANAGER` |
| 其他 `/api/**` | 已登录用户 |

### 请求属性

鉴权通过后，拦截器会往 request 中写入：

- `userId`
- `username`
- `role`

后续如果 Controller 或审计日志需要当前登录人，可以直接从 request attribute 取。

### 怎么审查

重点看 `AuthInterceptor.java`：

1. `isPublicRequest` 是否只放行登录、文档、OPTIONS。
2. `normalizeToken` 是否只接受 `Bearer ` 前缀。
3. `JwtUtils.isExpired(token)` 是否用于 Token 有效性校验。
4. `ROLE_RULES` 是否符合前端角色菜单设计。
5. `writeJson` 是否返回 `application/json;charset=UTF-8`。

重点看 `WebMvcConfig.java`：

1. 拦截路径是否为 `/api/**`。
2. 排除路径是否包含登录和文档。

重点看 `AuthInterceptorTest.java`：

已覆盖：

- 无 Token 返回 `401`
- 能源管理员访问系统管理返回 `403`
- 能源管理员访问能源模块放行
- `OPTIONS` 预检请求放行

### 前端影响

前端联调前必须做：

1. 登录后保存 `token`。
2. 后续真实接口统一加：

```http
Authorization: Bearer <token>
```

3. 处理 `401`：清 token，跳登录页。
4. 处理 `403`：提示无权限，或隐藏对应菜单入口。
5. 根据 `role/roles` 做菜单控制。

## 4. 统一异常返回示例

### 新增文件

- `接口异常返回示例.md`

### 内容

文档说明了后端统一返回结构：

```json
{
  "code": 400,
  "message": "请求参数错误",
  "data": null
}
```

覆盖示例：

- `401` 未登录或 Token 失效
- `403` 角色无权限
- `400` 参数校验失败
- `404` 业务数据不存在
- `500` 系统内部异常

### 怎么审查

1. 确认文档里的 `401/403` 文案和 `AuthInterceptor` 实际返回一致。
2. 确认角色规则和 `AuthInterceptor.ROLE_RULES` 一致。
3. 确认前端能根据 `code` 做统一提示。

## 5. 数据库集成测试

### 修改文件

- `AI-web/pom.xml`
- `AI-web/src/test/java/com/xq/integration/DatabaseIntegrationTest.java`

### 做了什么

在 `AI-web/pom.xml` 中新增 H2 测试依赖：

```xml
<dependency>
    <groupId>com.h2database</groupId>
    <artifactId>h2</artifactId>
    <scope>test</scope>
</dependency>
```

新增 `DatabaseIntegrationTest`，使用 H2 内存数据库：

```text
jdbc:h2:mem:ai_challenge;MODE=MySQL;DATABASE_TO_UPPER=false
```

测试内容：

1. 创建核心测试表：
   - `sys_user`
   - `sys_role`
   - `sys_user_role`
   - `energy_plan`
   - `energy_plan_detail`
   - `report_statistic`
2. 插入测试数据。
3. 验证用户角色关联查询。
4. 验证能源方案明细聚合。
5. 验证报表碳减排累计。

### 为什么用 H2，而不是直接连本机 MySQL

因为你这次说“先不联调”，所以不依赖本机数据库、不要求导入 SQL、不要求启动 MySQL。H2 内存库可以在 `mvn test` 中稳定跑，适合做基础数据库链路验证。

### 怎么审查

重点看 `DatabaseIntegrationTest.java`：

1. `createSchema` 是否覆盖了核心字段。
2. `seedData` 是否插入了足够验证关联和聚合的数据。
3. 三类查询是否符合业务使用方式：
   - 用户-角色 JOIN
   - 能源方案和明细 JOIN + SUM
   - 报表统计 SUM

### 后续可增强

如果后面开始真实联调，可以再加一类 `MySQL` profile 的集成测试，直接跑真实建表 SQL 和 seed 数据。但那一步会依赖本机 MySQL，不适合当前阶段默认执行。

## 6. 测试变化

### 新增测试

- `AuthInterceptorTest`
- `DatabaseIntegrationTest`

### 当前测试总数

```text
Tests run: 35, Failures: 0, Errors: 0, Skipped: 0
```

### 审查命令

```powershell
mvn test
```

如果只想看 Web 模块：

```powershell
mvn -pl AI-web test
```

## 7. 前端联调前重点检查

由于今天启用了真实鉴权，前端联调前必须确认：

1. 登录接口成功后保存 `data.token`。
2. 请求封装统一加 `Authorization: Bearer <token>`。
3. `401` 统一跳转登录页。
4. `403` 统一提示无权限。
5. 菜单按 `roles` 控制展示。
6. Swagger 或接口调试工具请求时也要手动加 Token。

## 8. 建议审查顺序

建议按这个顺序看：

1. `AuthInterceptor.java`
2. `AuthInterceptorTest.java`
3. `WebMvcConfig.java`
4. `接口异常返回示例.md`
5. `DatabaseIntegrationTest.java`
6. `AI-web/pom.xml`
7. `.editorconfig`
8. `pom.xml`
9. `启动说明.md`
10. `接口差距清单.md`

这样先看行为变化，再看测试保障，最后看文档和配置。

## 9. 风险点

### 风险 1：前端没有带 Token

现象：

- 登录以外接口返回 `401`

处理：

- 前端请求拦截器加 `Authorization: Bearer <token>`

### 风险 2：角色不匹配

现象：

- 能登录，但访问某些模块返回 `403`

处理：

- 检查登录用户的 `roles`
- 检查数据库 `sys_user_role/sys_role`
- 检查前端菜单是否隐藏无权限模块

### 风险 3：Swagger 调试接口失败

现象：

- Swagger 页面能打开，但具体 `/api/**` 请求返回 `401`

处理：

- 在 Swagger 或请求工具中补 `Authorization` 请求头

### 风险 4：终端仍显示中文乱码

现象：

- `Get-Content` 显示中文异常

处理：

```powershell
chcp 65001
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
```

文件本身仍是 UTF-8，可用 IDE 或 VS Code 打开确认。

## 10. 本次完成状态

| 项目 | 状态 |
|---|---|
| 源码中文乱码确认与编码配置 | 已完成 |
| 数据库集成测试 | 已完成 |
| 统一异常返回示例 | 已完成 |
| 登录 Token 拦截 | 已完成 |
| 角色鉴权 | 已完成 |
| 全量测试 | 已通过 |

