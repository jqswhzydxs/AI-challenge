# 数据库文件执行说明

> 适用项目：生产-能源交互式优化平台后端  
> 整理日期：2026-08-01  
> 数据库建议：MySQL 8.0，字符集 `utf8mb4`

## 1. 文件清单

| 文件 | 作用 | 数据性质 | 是否必需 |
| --- | --- | --- | --- |
| `数据库建表SQL.sql` | 创建业务表、索引和部分存量库升级 SQL | 表结构 | 必需 |
| `seed_demo_data.sql` | 初始化账号、角色、产线、设备、订单、主排产案例、排产历史兜底、能源历史兜底、告警、报表等 | 联调/演示数据 + 算法主案例摘要 | 建议执行 |
| `seed_steel_data.sql` | 导入 15 分钟粒度钢铁能源历史数据 | 算法组数据转换 | 需要历史能源样本时执行 |
| `seed_steel_1min.sql` | 导入 1 分钟粒度能源数据样本 | 算法组数据转换 | 需要分钟级曲线时执行 |
| `seed_mpc_control.sql` | 导入 MPC final 调控结果，日期 `2026-07-17` | 算法组 MPC 结果转换 | 需要实时调控页时执行 |
| `seed_mpc_hourly.sql` | 导入 MPC hourly 调控结果，日期 `2026-07-18` | 算法组 MPC 结果转换 | 需要 MPC 历史页时执行 |
| `reset_all.sql` | 清空主要业务表，便于重新导入 | 维护脚本 | 谨慎执行 |

## 2. 推荐执行顺序

新建本地联调库时，推荐顺序如下：

```sql
SOURCE D:/桌面/挑战杯/AI-challenge/数据库建表SQL.sql;
SOURCE D:/桌面/挑战杯/AI-challenge/seed_demo_data.sql;
SOURCE D:/桌面/挑战杯/AI-challenge/seed_steel_data.sql;
SOURCE D:/桌面/挑战杯/AI-challenge/seed_steel_1min.sql;
SOURCE D:/桌面/挑战杯/AI-challenge/seed_mpc_control.sql;
SOURCE D:/桌面/挑战杯/AI-challenge/seed_mpc_hourly.sql;
```

如果只是为了前后端快速联调，至少执行：

```sql
SOURCE D:/桌面/挑战杯/AI-challenge/数据库建表SQL.sql;
SOURCE D:/桌面/挑战杯/AI-challenge/seed_demo_data.sql;
SOURCE D:/桌面/挑战杯/AI-challenge/seed_mpc_control.sql;
SOURCE D:/桌面/挑战杯/AI-challenge/seed_mpc_hourly.sql;
```

## 3. 当前主案例口径

系统演示建议围绕 `2026-07-17` 这条主链路：

| 环节 | 当前数据 |
| --- | --- |
| 排产方案 | `production_schedule_plan.id = 4000000000000000101` |
| 算法文件 | `daily_plan_v3.2.json` |
| 优化前 EC | `14.00 kWh/t` |
| 优化后 EC | `13.2785 kWh/t` |
| EC 降低率 | `5.15%` |
| MAPE | `3.55%` |
| 仿真精度 | `96.45%` |
| ER | `100%` |
| MPC 锅炉负荷范围 | `20-80 MW` |
| MPC 汽机出力范围 | `5-30 MW` |

注意：`mape` 字段表示误差，前端展示“仿真精度”时应优先使用后端返回的 `simulationAccuracy`，即 `100 - mape`。

## 4. 数据来源分类

当前数据库数据分三类：

| 类型 | 典型数据 | 说明 |
| --- | --- | --- |
| 在线算法生成数据 | `algorithm_task`、`production_schedule_plan/detail`、`mpc_realtime_control` | 前端上传原始 CSV，后端调用 MATLAB 算法生成 `output.json` 后解析入库。 |
| 算法组数据转换入库 | `steel_energy_dataset`、`energy_realtime_data`、`mpc_realtime_control`、`production_schedule_plan/detail` | 算法组提供 CSV/JSON，后端转换为 SQL 或通过接口导入，主要作为联调和演示兜底。 |
| 后端派生数据 | `energy_plan/detail`、`joint_optimization_plan/timeseries`、`constraint_conflict` | 后端基于排产、能源、价格和约束规则生成，用于能源运行测算和协同评估。 |
| 联调演示数据 | `sys_user`、`production_order`、`energy_equipment`、`warning_record`、`report_statistic` | 用于登录、页面展示、历史列表和报表联调。 |

## 5. seed_demo_data.sql 说明

`seed_demo_data.sql` 现在包含以下内容：

- 默认用户与角色：`admin`、`dispatcher`、`energy`。
- 生产侧基础数据：产线、设备、订单。
- 能源侧基础数据：能源介质、锅炉、汽轮机、外购电接口。
- 主排产案例：`2026-07-17` 算法组 `daily_plan_v3.2.json` 导入结果。
- 排产历史兜底：`2026-07-15`、`2026-07-16` 两条联调模拟排产历史。
- 能源历史兜底：`2026-07-17` 的 24 小时能源历史样例，来源标识为 `demo_20260717_hourly`。
- 评价指标：MAPE、EC、ER 等算法报告指标。
- 首页告警、报表统计、操作日志。

该文件主要使用 `INSERT ... ON DUPLICATE KEY UPDATE`，本地联调环境可重复执行。正式库执行前建议先备份。

## 6. 注意事项

1. `seed_steel_data.sql` 会导入 15 分钟粒度能源数据，数据量较大。
2. `seed_steel_1min.sql` 文件约 40 MB，执行时间较长；如果只做页面联调，可先不执行。
3. `seed_mpc_control.sql` 和 `seed_mpc_hourly.sql` 会写入 `mpc_realtime_control`，核心调控值来自算法组结果文件，后端仅做字段补齐。
4. `reset_all.sql` 会清空主要业务表，除非需要重置环境，否则不要在正式库执行。
5. 当前峰谷电价时间口径按算法报告统一为：谷段 `00:00-08:00,22:00-24:00`，峰段 `08:00-22:00`；报告未设单独平段。
6. 真实生成流程不依赖 seed 文件：前端调用 `POST /api/production/schedules/generate-from-raw-data` 上传 CSV 后，后端会写入 `algorithm_task`，并在算法完成后写入排产、MPC 和评价指标相关表。

## 7. 验证建议

执行完成后可检查：

```sql
SELECT COUNT(*) FROM sys_user;
SELECT COUNT(*) FROM production_schedule_plan;
SELECT COUNT(*) FROM production_schedule_detail WHERE schedule_id = 4000000000000000101;
SELECT COUNT(*) FROM energy_realtime_data WHERE source = 'demo_20260717_hourly';
SELECT COUNT(*) FROM mpc_realtime_control;
SELECT * FROM evaluation_metric ORDER BY calculate_time DESC LIMIT 5;
```

如果 `production_schedule_plan`、`energy_realtime_data` 或 `mpc_realtime_control` 为空，对应页面会缺少历史数据或实时调控数据。
