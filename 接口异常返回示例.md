# 接口异常返回示例

后端统一返回 `code/message/data` 结构。业务异常、参数校验异常、鉴权异常都使用同一 JSON 形态，前端可以优先根据 `code` 做提示和跳转。

## 通用格式

```json
{
  "code": 400,
  "message": "请求参数错误",
  "data": null
}
```

## 未登录或 Token 失效

HTTP 状态：`401`

```json
{
  "code": 401,
  "message": "未登录或 Token 已失效",
  "data": null
}
```

前端处理建议：清理本地 token，跳转登录页。

## 角色无权限

HTTP 状态：`403`

```json
{
  "code": 403,
  "message": "当前角色无权访问该接口",
  "data": null
}
```

当前角色规则：

| 路径前缀 | 允许角色 |
|---|---|
| `/api/system/**` | `SYSTEM_ADMIN` |
| `/api/production/**` | `SYSTEM_ADMIN`, `PRODUCTION_DISPATCHER` |
| `/api/energy/**` | `SYSTEM_ADMIN`, `ENERGY_MANAGER` |
| 其他 `/api/**` | 已登录用户 |

## 参数校验失败

HTTP 状态：通常为 `200`，响应体 `code=400`。

```json
{
  "code": 400,
  "message": "username cannot be blank",
  "data": null
}
```

## 业务数据不存在

HTTP 状态：通常为 `200`，响应体 `code=404`。

```json
{
  "code": 404,
  "message": "该日期能源方案不存在",
  "data": null
}
```

## 系统内部异常

HTTP 状态：通常为 `200`，响应体 `code=500`。

```json
{
  "code": 500,
  "message": "系统内部错误",
  "data": null
}
```
